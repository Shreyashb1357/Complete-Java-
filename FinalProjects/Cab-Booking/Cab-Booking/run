#!/bin/bash

echo "Building Spring Boot project (skipping tests)..."

if mvn clean package -DskipTests; then
    echo "Build successful. Starting application..."
    java "$@" -jar target/*.jar
else
    echo "Build failed. Application not started."
fi
